@model IEnumerable<Tanger_API.Models.AirPollution>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocationId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pollutant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PollutantLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timestamp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pollutant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PollutantLevel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Timestamp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AirPollutionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AirPollutionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AirPollutionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
